vibeland\docs\tasks.md
```

# Задачи проекта

## 1. Создание структуры проекта
- [x] Инициализировать проект Nuxt.js.
- [x] Установить Tailwind CSS (v4) через Bun.
- [x] Настроить базовую конфигурацию Nuxt.js.
- [x] Создать директории `components`, `themes`, `content`, `docs`.

## 2. Разработка сайта-визитки
- [x] Создать компонент для отображения информации о Георгии (имя, город, возраст, навыки).
- [x] Разработать компонент "Навигация" с ссылками на разделы сайта (залитый фон, hover-анимации для ссылок и кнопок).
- [x] Создать компонент "Обо мне" с кратким описанием и фотографией.
- [x] Реализовать компонент "Контакты" с формой обратной связи (отправка на сервер).
- [x] Дописать `Gallery`: добавлен полноэкранный просмотр медиа (картинки/видео) через оверлей + `Teleport`, навигация (след/пред), закрытие по `Esc` и клику по фону, возврат фокуса на исходный элемент, aria (`role="dialog"`, `aria-modal`, подпись).
- [x] Создать новую страницу и добавить ссылку на неё в `SiteNav` (desktop + mobile), используя `NuxtLink` для роутов и якоря только для секций внутри главной.
- [x] Настроить переключение тем (светлая/тёмная) через Tailwind.
  - Уточнение: переключатель темы вынесен в `app/app.vue`, чтобы он был доступен на всех страницах (глобальный UI-элемент оболочки).
  - Уточнение: поправлено наложение на шапку — глобальный переключатель сдвинут ниже, чтобы не перекрывать `sticky`-header.
- Добавить стилизацию с использованием CSS-переменных.
- Использовать современный минималистичный дизайн с акцентом на типографику и плавные анимации.
- [x] Реализовать серверное сохранение обращений из формы контактов через Nuxt Server (Nitro) API + SQLite (Nitro роутинг: `server/api` в корне): создан `contacts.post`, добавлена валидация, сохранение в SQLite и подключение формы к API.

## 3. Создание мини-раздела с блогом
- [x] Настроить @nuxt/content и @nuxt/studio для работы с markdown.
  - Обновлено: Nuxt Content переведён на v3 и заведён `content.config.ts` с коллекцией `projects` (источник: `content/projects/**/*.{md,mdc}`).
  - Добавлено: schema для frontmatter (`title`, `description`, `date`, `tags`, `draft`) чтобы поля индексировались и были доступны в запросах.
  - Исправлено: список и детальная страница переведены на `queryCollection("projects")` (Nuxt Content v3), чтобы не было 404 на `/__nuxt_content/content/query`.
  - Исправлено: Nuxt Studio работает с корнем `content/` (repository `rootDir: "content"`), а для стабилизации поведения при создании документов добавлена корневая запись `content/index.md`.
  - Исправлено: падение приложения при открытии детальной страницы из-за destructuring slot props (`doc`) — теперь детальная страница сама загружает документ через query и аккуратно обрабатывает pending/error/not found.
- [x] Создать шаблон для отображения списка проектов (страница списка реализована: `app/pages/projects/index.vue`).
- [x] Реализовать страницу для отображения контента из markdown (детальная страница реализована: `app/pages/projects/[...slug].vue`).
- [x] Починить создание документов в Studio: удалена неверная конфигурация Studio (создавала несопоставимый fsPath), синхронизация работает с `content/` через `rootDir: "content"`.

## 4. Оптимизация и документация
- [x] Вынести общие стили в `themes` (глобальные стили перенесены в `themes/main.css`, подключение обновлено в `nuxt.config.ts`).
- [x] Обновить `.gitignore` для исключения ненужных файлов (актуальные исключения уже присутствуют: `.data`, `.nuxt`, `node_modules`, `dist`, `.output`).
- Вести прогресс в `docs/tasks.md`.

## 5. Тестирование и финализация
- Проверить корректность работы всех страниц.
- Убедиться в соответствии принципам SOLID и KISS.
- Оптимизировать сайт для SEO (мета-теги, структура, производительность).
- Подготовить проект к развертыванию.
